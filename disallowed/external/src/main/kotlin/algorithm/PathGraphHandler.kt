package algorithm

import path.PathGraph

class PathGraphHandler(private val graph: PathGraph, path: String) : AlgorithmHandler(path) {
    override fun build() {
        TODO("Not yet implemented")
    }
}